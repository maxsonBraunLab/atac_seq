---
__default__:
  time: '06:00:00'
  nodes: 1
  partition: exacloud
  job-name: "{rule}.{wildcards.sample}"
  output: "logs/{rule}/{wildcards.sample}.out"
  error: "logs/{rule}/{wildcards.sample}.err"
  cpus-per-task: 8
  mem: 4G
align:
  cpus-per-task: 16
  mem: 32G
# sortbam:
#   job-name: "sortbam.{wildcards.sample}"
#   output: "logs/sortbam/{wildcards.sample}.out"
#   error: "logs/sortbam/{wildcards.sample}.err"
fastqc:
  job-name: "fastqc.{wildcards.sample}"
  output: "logs/fastqc/{wildcards.sample}.out"
  error: "logs/fastqc/{wildcards.sample}.err"
fastqc_screen:
  job-name: "scrn.{wildcards.sample}"
  output: "logs/fastq_screen/{wildcards.sample}.out"
  error: "logs/fastq_screen/{wildcards.sample}.err"
removeMitochondrial:
  job-name: "mto.{wildcards.sample}"
  output: "logs/removeMitochondrial/{wildcards.sample}.out"
  error: "logs/removeMitochondrial/{wildcards.sample}.err"